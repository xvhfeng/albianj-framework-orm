对于Rant中的的FileName来说，它配置的时候是可以有前缀的。所以有以下几种情况：
1. 没有任何的前缀，直接就配置名字，或者路径带名字，那么将会在程序启动的config path路径下查找
2. ‘#’ 前缀，表示相对路径，它前面可以带有一个路径或者不带，带有路径即对于该路径的相对路径，不带即对于config path，例如path#../../service.xml
3. ‘@’ 前缀，表示相对于根路径












